# Cloudflare Pages Configuration
# This file is optional - deployment can be done via CLI without it

name = "roguelike"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"
