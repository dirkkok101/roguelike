# Cloudflare Pages Configuration
# This file is optional - deployment can be done via CLI without it

name = "roguelike"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"

# Build configuration (optional - can override via CLI)
[build]
command = "npm run build"

# Preview environment (optional)
[env.preview]
compatibility_date = "2024-01-01"

# Production environment (optional)
[env.production]
compatibility_date = "2024-01-01"
